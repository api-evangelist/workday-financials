naftiko: 1.0.0-alpha2
info:
  label: Workday Financials Workday Revenue Management API — Revenue Schedules
  description: 'Workday Financials Workday Revenue Management API — Revenue Schedules. 2 operations. Lead operation: Workday
    Financials List revenue schedules. Self-contained Naftiko capability covering one Workday Financials business surface.'
  tags:
  - Workday Financials
  - Revenue Schedules
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    WORKDAY_FINANCIALS_API_KEY: WORKDAY_FINANCIALS_API_KEY
capability:
  consumes:
  - type: http
    namespace: revenue-management-revenue-schedules
    baseUri: https://{tenant}.workday.com/api/revenueManagement/v38.2
    description: Workday Financials Workday Revenue Management API — Revenue Schedules business capability. Self-contained,
      no shared references.
    resources:
    - name: revenueSchedules
      path: /revenueSchedules
      operations:
      - name: listrevenueschedules
        method: GET
        description: Workday Financials List revenue schedules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: revenueSchedules-id
      path: /revenueSchedules/{id}
      operations:
      - name: getrevenueschedule
        method: GET
        description: Workday Financials Get a revenue schedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.WORKDAY_FINANCIALS_API_KEY}}'
  exposes:
  - type: rest
    namespace: revenue-management-revenue-schedules-rest
    port: 8080
    description: REST adapter for Workday Financials Workday Revenue Management API — Revenue Schedules. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/revenueschedules
      name: revenueschedules
      description: REST surface for revenueSchedules.
      operations:
      - method: GET
        name: listrevenueschedules
        description: Workday Financials List revenue schedules
        call: revenue-management-revenue-schedules.listrevenueschedules
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/revenueschedules/{id}
      name: revenueschedules-id
      description: REST surface for revenueSchedules-id.
      operations:
      - method: GET
        name: getrevenueschedule
        description: Workday Financials Get a revenue schedule
        call: revenue-management-revenue-schedules.getrevenueschedule
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: revenue-management-revenue-schedules-mcp
    port: 9090
    transport: http
    description: MCP adapter for Workday Financials Workday Revenue Management API — Revenue Schedules. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: workday-financials-list-revenue-schedules
      description: Workday Financials List revenue schedules
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: revenue-management-revenue-schedules.listrevenueschedules
      outputParameters:
      - type: object
        mapping: $.
    - name: workday-financials-get-revenue-schedule
      description: Workday Financials Get a revenue schedule
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: revenue-management-revenue-schedules.getrevenueschedule
      outputParameters:
      - type: object
        mapping: $.
