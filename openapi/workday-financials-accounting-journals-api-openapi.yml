openapi: 3.1.0
info:
  title: Workday Financials Workday Cash Management Account Reconciliations Accounting Journals API
  description: API for managing cash positions, bank accounts, transactions, and cash forecasting within Workday Cash Management.
  version: v38.2
  contact:
    name: Workday Support
    url: https://www.workday.com/en-us/company/latest/support.html
  termsOfService: https://www.workday.com/en-us/legal.html
servers:
- url: https://{tenant}.workday.com/api/cashManagement/v38.2
  description: Workday Cash Management Production
  variables:
    tenant:
      description: Workday tenant identifier
      default: your-tenant
security:
- bearerAuth: []
tags:
- name: Accounting Journals
  description: Access and manage accounting journals
paths:
  /accountingJournals:
    get:
      operationId: listAccountingJournals
      summary: Workday Financials List accounting journals
      description: Retrieve a collection of accounting journals.
      tags:
      - Accounting Journals
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/offset'
      responses:
        '200':
          description: Accounting journals retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  total:
                    type: integer
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AccountingJournal'
        '401':
          $ref: '#/components/responses/Unauthorized'
components:
  schemas:
    AccountingJournal:
      type: object
      properties:
        id:
          type: string
          description: Workday ID (WID) for the accounting journal
        name:
          type: string
          description: Name of the accounting journal
        journalType:
          type: string
          description: Type of accounting journal
        active:
          type: boolean
          description: Whether the journal is active
  responses:
    Unauthorized:
      description: Authentication credentials are missing or invalid
  parameters:
    offset:
      name: offset
      in: query
      description: Number of results to skip for pagination
      schema:
        type: integer
        default: 0
    limit:
      name: limit
      in: query
      description: Maximum number of results to return
      schema:
        type: integer
        default: 20
        maximum: 100
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: OAuth 2.0 bearer token obtained from Workday authentication
externalDocs:
  description: Workday Cash Management API Documentation
  url: https://community.workday.com/sites/default/files/file-hosting/productionapi/index.html
