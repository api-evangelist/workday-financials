openapi: 3.2.0
info:
  title: Workday Financials Workday Revenue Management Revenue…
  description: API for managing revenue recognition, contracts, billing, and revenue accounting in compliance with ASC 606 and IFRS 15 within Workday Revenue Management.
  version: v38.2
  contact:
    name: Workday Support
    url: https://www.workday.com/en-us/company/latest/support.html
  termsOfService: https://www.workday.com/en-us/legal.html
servers:
- url: https://{tenant}.workday.com/api/revenueManagement/v38.2
  description: Workday Revenue Management Production
  variables:
    tenant:
      description: Workday tenant identifier
      default: your-tenant
security:
- bearerAuth: []
tags:
- name: Revenue Recognition
  description: Revenue recognition events and processing
paths:
  /revenueRecognitionEvents:
    get:
      operationId: listRevenueRecognitionEvents
      summary: Workday Financials List revenue recognition events
      description: Retrieve a collection of revenue recognition events.
      tags:
      - Revenue Recognition
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/offset'
      - name: fromDate
        in: query
        description: Filter events from this date
        schema:
          type: string
          format: date
      - name: toDate
        in: query
        description: Filter events to this date
        schema:
          type: string
          format: date
      responses:
        '200':
          description: Revenue recognition events retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  total:
                    type: integer
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/RevenueRecognitionEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
components:
  parameters:
    limit:
      name: limit
      in: query
      description: Maximum number of results to return
      schema:
        type: integer
        default: 20
        maximum: 100
    offset:
      name: offset
      in: query
      description: Number of results to skip for pagination
      schema:
        type: integer
        default: 0
  responses:
    Unauthorized:
      description: Authentication credentials are missing or invalid
  schemas:
    ContractRef:
      type: object
      properties:
        id:
          type: string
          description: Workday ID of the revenue contract
        descriptor:
          type: string
          description: Display name of the contract
    CurrencyRef:
      type: object
      properties:
        id:
          type: string
          description: Workday ID of the currency
        descriptor:
          type: string
          description: Currency code (e.g., USD, EUR)
    RevenueRecognitionEvent:
      type: object
      properties:
        id:
          type: string
          description: Workday ID (WID) for the recognition event
        revenueContract:
          $ref: '#/components/schemas/ContractRef'
        recognitionDate:
          type: string
          format: date
          description: Date revenue was recognized
        amount:
          type: number
          format: double
          description: Recognized revenue amount
        currency:
          $ref: '#/components/schemas/CurrencyRef'
        recognitionType:
          type: string
          description: Type of recognition event
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: OAuth 2.0 bearer token obtained from Workday authentication
externalDocs:
  description: Workday Revenue Management API Documentation
  url: https://community.workday.com/sites/default/files/file-hosting/productionapi/index.html
