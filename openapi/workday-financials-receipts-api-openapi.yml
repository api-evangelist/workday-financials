openapi: 3.2.0
info:
  title: Workday Financials Receipts API
  version: v38.2
  contact:
    name: Workday Support
    url: https://www.workday.com/en-us/company/latest/support.html
  termsOfService: https://www.workday.com/en-us/legal.html
  description: 'Operations tagged Receipts across 2 of this provider''s published API definitions: workday-financials-expenses-openapi.yml, workday-financials-procurement-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://{tenant}.workday.com/api/expenses/v38.2
  description: Workday Expenses Production
  variables:
    tenant:
      description: Workday tenant identifier
      default: your-tenant
- url: https://{tenant}.workday.com/api/procurement/v38.2
  description: Workday Procurement Production
  variables:
    tenant:
      description: Workday tenant identifier
      default: your-tenant
security:
- bearerAuth: []
tags:
- name: Receipts
  description: Upload and manage expense receipts
paths:
  /receipts:
    post:
      operationId: uploadReceipt
      summary: Workday Financials Upload a receipt
      description: Upload a receipt image for attachment to an expense item.
      tags:
      - Receipts
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
              - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Receipt image file
                description:
                  type: string
                  description: Description of the receipt
      responses:
        '201':
          description: Receipt uploaded successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Receipt'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
    servers:
    - url: https://{tenant}.workday.com/api/expenses/v38.2
      description: Workday Expenses Production
      variables:
        tenant:
          description: Workday tenant identifier
          default: your-tenant
  /receipts/{id}:
    get:
      operationId: getReceipt
      summary: Workday Financials Get a receipt
      description: Retrieve details of a specific receipt.
      tags:
      - Receipts
      parameters:
      - $ref: '#/components/parameters/resourceId'
      responses:
        '200':
          description: Receipt details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Receipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      operationId: deleteReceipt
      summary: Workday Financials Delete a receipt
      description: Delete a receipt that is not attached to a submitted expense report.
      tags:
      - Receipts
      parameters:
      - $ref: '#/components/parameters/resourceId'
      responses:
        '204':
          description: Receipt deleted successfully
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    servers:
    - url: https://{tenant}.workday.com/api/expenses/v38.2
      description: Workday Expenses Production
      variables:
        tenant:
          description: Workday tenant identifier
          default: your-tenant
  /goodsReceipts:
    get:
      operationId: listGoodsReceipts
      summary: Workday Financials List goods receipts
      description: Retrieve a collection of goods receipts.
      tags:
      - Receipts
      parameters:
      - $ref: '#/components/parameters/limit'
      - $ref: '#/components/parameters/offset'
      responses:
        '200':
          description: Goods receipts retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  total:
                    type: integer
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/GoodsReceipt'
        '401':
          $ref: '#/components/responses/Unauthorized'
    servers:
    - url: https://{tenant}.workday.com/api/procurement/v38.2
      description: Workday Procurement Production
      variables:
        tenant:
          description: Workday tenant identifier
          default: your-tenant
components:
  schemas:
    Receipt:
      type: object
      properties:
        id:
          type: string
          description: Workday ID (WID) for the receipt
        fileName:
          type: string
          description: Name of the uploaded receipt file
        contentType:
          type: string
          description: MIME type of the receipt file
        uploadedOn:
          type: string
          format: date-time
          description: When the receipt was uploaded
        description:
          type: string
          description: Description of the receipt
    GoodsReceipt:
      type: object
      properties:
        id:
          type: string
          description: Workday ID (WID) for the goods receipt
        receiptNumber:
          type: string
          description: Goods receipt number
        purchaseOrder:
          $ref: '#/components/schemas/PurchaseOrderRef'
        receiptDate:
          type: string
          format: date
          description: Date goods were received
        status:
          type: string
          enum:
          - Received
          - Partially Received
          - Returned
          description: Receipt status
    PurchaseOrderRef:
      type: object
      properties:
        id:
          type: string
          description: Workday ID of the purchase order
        descriptor:
          type: string
          description: Display name of the purchase order
  responses:
    Unauthorized:
      description: Authentication credentials are missing or invalid
    NotFound:
      description: The requested resource was not found
    BadRequest:
      description: The request was malformed or contained invalid data
  parameters:
    resourceId:
      name: id
      in: path
      required: true
      description: Unique identifier of the resource (WID)
      schema:
        type: string
    limit:
      name: limit
      in: query
      description: Maximum number of results to return
      schema:
        type: integer
        default: 20
        maximum: 100
    offset:
      name: offset
      in: query
      description: Number of results to skip for pagination
      schema:
        type: integer
        default: 0
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: OAuth 2.0 bearer token obtained from Workday authentication
x-refined-from:
- workday-financials-expenses-openapi.yml
- workday-financials-procurement-openapi.yml
